# WSL and Windows compatibility
legacy-peer-deps=true
auto-install-peers=true

# Resolve dependency conflicts
strict-peer-deps=false

# Performance improvements for WSL
prefer-offline=true
audit=false

# Prevent EACCES errors in WSL
unsafe-perm=true

# Fix symbolic link issues in WSL
no-bin-links=true